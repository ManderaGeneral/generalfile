name: Testing

on:
  push:
    branches: master

jobs:

  foo:
    runs-on: ubuntu-latest
    outputs:
      status: ${{ steps.first.conclusion }}
    steps:
      - id: first
        run: |
          echo a

  bar:
    needs: foo
    runs-on: ubuntu-latest
    if: needs.foo.outputs.status == 'success'
    steps:
    - name: second
      run: |
        echo b


















